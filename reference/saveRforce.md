# saveRDS function for Rforce

saveRDS function for Rforce

## Usage

``` r
saveRforce(forest, file, ...)
```

## Arguments

- file:

  A character string giving the name of the file where the object should
  be saved

- object:

  An Rforce object
